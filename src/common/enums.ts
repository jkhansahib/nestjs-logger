/**
 * Central place for shared enums used across the app.
 * Add more enums here as needed.
 */

export enum Brand {
  PHONER = 0,
  VUMBER = 1,
  CALL_RECORDER = 2,
  FAX_ZAP = 3,
  TEXT_VAULT = 4,
}

export enum Platform {
  IOS = 0,
  ANDROID = 1,
  WEB = 2,
  MAC = 3,
  WINDOWS = 4,
}

export enum AuthProvider{
    supabase = 0
}
